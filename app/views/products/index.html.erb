<% content_for :title, "Products - Potlift8" %>
<div class="px-4 sm:px-6 lg:px-8" data-controller="bulk-actions">
  <!-- Page Header -->
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-3xl font-semibold leading-6 text-gray-900">Products</h1>
      <p class="mt-2 text-sm text-gray-700">
        Manage your inventory products, including sellable items, configurable products, and bundles.
      </p>
    </div>
    <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none flex gap-3">
      <%= render Ui::ButtonComponent.new(
        variant: :secondary,
        href: products_path(format: :csv, **request.query_parameters.except(:format)),
        data: { turbo_frame: "_top" },
        icon: '<svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>',
        icon_position: :left
      ) do %>
        Export CSV
      <% end %>

      <%= render Ui::ButtonComponent.new(
        variant: :secondary,
        href: new_import_path,
        icon: '<svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM6.293 6.707a1 1 0 010-1.414l3-3a1 1 0 011.414 0l3 3a1 1 0 01-1.414 1.414L11 5.414V13a1 1 0 11-2 0V5.414L7.707 6.707a1 1 0 01-1.414 0z" clip-rule="evenodd" /></svg>',
        icon_position: :left
      ) do %>
        Import
      <% end %>

      <%= render Ui::ButtonComponent.new(
        variant: :primary,
        href: new_product_path,
        icon: '<svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M10.75 4.75a.75.75 0 00-1.5 0v4.5h-4.5a.75.75 0 000 1.5h4.5v4.5a.75.75 0 001.5 0v-4.5h4.5a.75.75 0 000-1.5h-4.5v-4.5z" /></svg>',
        icon_position: :left
      ) do %>
        Add Product
      <% end %>
    </div>
  </div>

  <!-- Search and Filters -->
  <div class="mt-8">
    <%= form_with url: products_path, method: :get, data: { turbo_frame: 'products_table' }, class: "space-y-4" do |f| %>
      <!-- Main search and type filter row -->
      <div class="sm:flex sm:gap-x-4">
        <%= render Ui::SearchInputComponent.new(
          name: :q,
          value: params[:q],
          placeholder: "Search products by name or SKU...",
          label: "Search products by name or SKU"
        ) %>

        <div class="mt-4 sm:mt-0" data-controller="auto-submit">
          <%= f.select :type,
              options_for_select([
                ['All Types', ''],
                ['Sellable', 'sellable'],
                ['Configurable', 'configurable'],
                ['Bundle', 'bundle']
              ], params[:type]),
              {},
              class: "block w-full rounded-md border-0 py-1.5 pl-3 pr-10 text-gray-900 ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-blue-600 sm:text-sm sm:leading-6",
              aria: { label: "Filter by product type" },
              data: { action: "change->auto-submit#submit" } %>
        </div>

        <!-- Label filter dropdown -->
        <div class="mt-4 sm:mt-0 relative" data-controller="dropdown">
          <button
            type="button"
            data-action="click->dropdown#toggle"
            data-dropdown-target="button"
            aria-expanded="false"
            aria-haspopup="true"
            class="inline-flex w-full justify-between gap-x-1.5 rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
            <span>Filter by Label</span>
            <svg class="h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
            </svg>
          </button>

          <div
            data-dropdown-target="menu"
            class="hidden absolute right-0 z-10 mt-2 w-72 origin-top-right rounded-md bg-white shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none max-h-96 overflow-y-auto transition-all duration-200 starting:opacity-0 starting:scale-95"
            role="menu"
            aria-orientation="vertical">
            <div class="py-1">
              <% if @available_labels.any? %>
                <% @available_labels.each do |label| %>
                  <%= link_to products_path(label_id: label.id, q: params[:q], type: params[:type]),
                      class: "flex items-center justify-between px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:text-gray-900",
                      role: "menuitem" do %>
                    <span class="font-medium"><%= label.name %></span>
                    <span class="text-gray-500">(<%= @label_product_counts[label.id] || 0 %>)</span>
                  <% end %>

                  <% if label.sublabels.any? %>
                    <% label.sublabels.each do |sublabel| %>
                      <%= link_to products_path(label_id: sublabel.id, q: params[:q], type: params[:type]),
                          class: "flex items-center justify-between px-4 py-2 pl-8 text-sm text-gray-600 hover:bg-gray-100 hover:text-gray-900",
                          role: "menuitem" do %>
                        <span><%= sublabel.name %></span>
                        <span class="text-gray-400">(<%= @label_product_counts[sublabel.id] || 0 %>)</span>
                      <% end %>
                    <% end %>
                  <% end %>
                <% end %>
              <% else %>
                <div class="px-4 py-2 text-sm text-gray-500">No labels available</div>
              <% end %>
            </div>
          </div>
        </div>

        <div class="mt-4 sm:mt-0 flex gap-x-2">
          <%= f.submit "Search", class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
          <% if params[:q].present? || params[:type].present? || params[:label_id].present? %>
            <%= link_to "Clear", products_path, class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
          <% end %>
        </div>
      </div>

      <!-- Active filters display -->
      <% if @current_label.present? %>
        <div class="flex flex-wrap gap-2 items-center">
          <span class="text-sm text-gray-700">Active filters:</span>
          <span class="inline-flex items-center gap-x-1.5 rounded-full bg-blue-100 px-3 py-1 text-sm font-medium text-blue-800">
            <svg class="h-2 w-2 fill-blue-500" viewBox="0 0 6 6" aria-hidden="true">
              <circle cx="3" cy="3" r="3" />
            </svg>
            <%= @current_label.full_name %>
            <%= link_to products_path(q: params[:q], type: params[:type]),
                class: "ml-1 inline-flex h-5 w-5 items-center justify-center rounded-full hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-offset-2 focus:ring-offset-blue-100",
                aria: { label: "Remove label filter" } do %>
              <svg class="h-3 w-3" fill="currentColor" viewBox="0 0 20 20">
                <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
              </svg>
            <% end %>
          </span>
        </div>
      <% end %>
    <% end %>
  </div>

  <!-- Bulk Actions Toolbar (hidden by default) -->
  <div class="mt-4 hidden" data-bulk-actions-target="toolbar">
    <div class="rounded-md bg-blue-50 p-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a.75.75 0 000 1.5h.253a.25.25 0 01.244.304l-.459 2.066A1.75 1.75 0 0010.747 15H11a.75.75 0 000-1.5h-.253a.25.25 0 01-.244-.304l.459-2.066A1.75 1.75 0 009.253 9H9z" clip-rule="evenodd" />
          </svg>
          <p class="ml-3 text-sm font-medium text-blue-800">
            <span data-bulk-actions-target="count">0</span> product(s) selected
          </p>
        </div>
        <div class="flex gap-x-2">
          <button
            type="button"
            data-action="click->bulk-actions#openLabelEditor"
            class="rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
            <svg class="inline-block h-4 w-4 mr-1" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.5 3A2.5 2.5 0 003 5.5v2.879a2.5 2.5 0 00.732 1.767l6.5 6.5a2.5 2.5 0 003.536 0l2.878-2.878a2.5 2.5 0 000-3.536l-6.5-6.5A2.5 2.5 0 008.38 3H5.5zM6 7a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
            </svg>
            Edit Labels
          </button>
          <button
            type="button"
            data-action="click->bulk-actions#bulkExport"
            class="rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
            Export CSV
          </button>
          <button
            type="button"
            data-action="click->bulk-actions#bulkDelete"
            class="rounded-md bg-red-600 px-2.5 py-1.5 text-sm font-semibold text-white shadow-sm hover:bg-red-500">
            Delete Selected
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Products Table -->
  <%#
  Example usage with Turbo Frames + Skeleton Loading:

  turbo_frame_tag "products_table", src: products_path, loading: :lazy do
    render Shared::SkeletonTableComponent.new(
      rows: 10,
      columns: ['SKU', 'Name', 'Type', 'Status', 'Inventory', 'Actions']
    )
  end
  %>
  <%= render Products::TableComponent.new(
    products: @products,
    pagy: @pagy,
    current_sort: params[:sort],
    current_direction: params[:direction],
    search_query: params[:q],
    has_filters: params[:type].present? || params[:label_id].present?
  ) %>

  <!-- Bulk Label Editor Modal -->
  <%= render Products::BulkLabelEditorComponent.new(
    labels: @available_labels
  ) %>
</div>
