<% if flash[:alert].present? %>
  <%= turbo_stream.update "flash" do %>
    <%= render FlashComponent.new(flash: flash.to_h) %>
  <% end %>
<% else %>
  <%= turbo_stream.remove dom_id(@storage) %>

  <%= turbo_stream.update "flash" do %>
    <%= render FlashComponent.new(flash: flash.to_h) %>
  <% end %>
<% end %>
