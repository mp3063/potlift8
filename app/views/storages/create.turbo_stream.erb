<%= turbo_stream.remove "storages-empty-state" %>

<%= turbo_stream.append "storages-grid" do %>
  <%= render "storage_card", storage: @storage %>
<% end %>

<%= turbo_stream.update "flash" do %>
  <%= render FlashComponent.new(flash: flash.to_h) %>
<% end %>
